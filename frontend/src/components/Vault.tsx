import React from 'react';
import styles from './styles/Vault.module.scss';

const Vault: React.FC = () => {
  return (
    <main className={styles.main}>
      <h1>Vault</h1>
      <p>This is the vault page. Add vault related UI here.</p>
    </main>
  );
};

export default Vault;
